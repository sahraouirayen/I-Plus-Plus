{% extends 'back.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block body %}
{% block content %}
    <h1>Evenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdEvent</th>
                <td>{{ evenement.idEvent }}</td>
            </tr>
            <tr>
                <th>NomEvent</th>
                <td>{{ evenement.nomEvent }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ evenement.date ? evenement.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ evenement.lieu }}</td>
            </tr>
            <tr>
                <th>DescEvent</th>
                <td>{{ evenement.descEvent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('affichage') }}" class="btn btn-warning">back to list</a>

    <a href="{{ path('evenement_edit', {'idEvent': evenement.idEvent}) }}" class="btn btn-primary">edit</a>

    {{ include('evenement/_delete_form.html.twig') }}
{% endblock %}
{% endblock %}
