{% extends 'base.html.twig' %}

{% block title %}Typereclamations{% endblock %}

{% block body %}
    <h1>Typereclamations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typereclamation.id }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ typereclamation.niveau }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_typereclamations_index') }}">back to list</a>

    <a href="{{ path('app_typereclamations_edit', {'id': typereclamation.id}) }}">edit</a>

    {{ include('typereclamations/_delete_form.html.twig') }}
{% endblock %}
