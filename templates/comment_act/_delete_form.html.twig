<form method="post" action="{{ path('comment_act_delete', {'idComment': comment_act.idComment}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment_act.idComment) }}">
    <button class="btn">Delete</button>
</form>
