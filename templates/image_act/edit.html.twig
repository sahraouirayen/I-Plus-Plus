{% extends 'base.html.twig' %}

{% block title %}Edit ImageAct{% endblock %}

{% block body %}
    <h1>Edit ImageAct</h1>

    {{ include('image_act/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('image_act_index') }}">back to list</a>

    {{ include('image_act/_delete_form.html.twig') }}
{% endblock %}
