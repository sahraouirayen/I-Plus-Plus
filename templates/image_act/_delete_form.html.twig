<form method="post" action="{{ path('image_act_delete', {'idImage': image_act.idImage}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_act.idImage) }}">
    <button class="btn">Delete</button>
</form>
