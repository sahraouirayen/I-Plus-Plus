{% extends 'base.html.twig' %}

{% block title %}Promotion{% endblock %}

{% block body %}
    <h1>Promotion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPromotion</th>
                <td>{{ promotion.idPromotion }}</td>
            </tr>
            <tr>
                <th>PrixPromotion</th>
                <td>{{ promotion.prixPromotion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('promotion_index') }}">back to list</a>

    <a href="{{ path('promotion_edit', {'idPromotion': promotion.idPromotion}) }}">edit</a>

    {{ include('promotion/_delete_form.html.twig') }}
{% endblock %}
